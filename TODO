GOALS FOR THIS WEEKEND:
  \ make a proper scene loader so entities can be added to the scene class
  \ image drawing wherever i want, however many times i want it to
  \ moving image around with keyboard
  motion system ( triggerize it, make sys call available in lua, expose motion's more specialized funcs to lua)

TODAY:
  \  make 3 triggers send data for Trigger.cpp to fill 2 arrays with 
  \  share that array with FFI in bicycle.lua
  \  impl, test, and debug Image, all in C++
  Genome (for foreground objects)
    \  img
    \  pos (multiple, inject)
    vel
    personality (respond to ITC)
    anim
    coll
    sound
    song
    scene (fg, bg): impl, test, and debug all 3 types
    custom
  fix the annoying window and hbad terminal closing state bugs
  ~Grids next (making great progress! need to make it easy to add foreground objs, then collision fg/bg info)
  make a guy move around the screen
  parallax, i'm not waiting anymore on this
  looping bg
  glued bg 
  motion next
  animated motion next
  animated background next
  dice next
  make an NPC move at random
  make an NPC move based on random timers
  make them collide and react
  make them collide with bg, fast and slow
  play a sound when they collide
  play a song
  switch scenes when you collide with a staircase
  Health bar
  random RPG battle with music and everything, then back to normal scene

====================================
*LINKAGES BETWEEN ENGINE AND SCRIPTS
====================================

  Eat the one-pound burger one bite at a time. Cross these off as you go.

      C++                   LUA
      ---                   ---
  REGISTER
  ========
  \world set/get     <-  worldSet/Get    (WARNING: C++ side needs explicit wrappers for every World  subtype)
  \push dialogue     <-  pushDialogue    (WARNING: C++ side needs explicit wrappers for every Window subtype)
  \push text menu    <-  pushTextMenu    (WARNING: C++ side needs explicit wrappers for every Window subtype)
  push grid         <-  pushGrid        (WARNING: C++ side needs explicit wrappers for every Window subtype)
  \pop window        <-  popWindow
  strt/stop/ps/unps <-  sys  <- wrappers for easier calls (provided by bicycle)
     * timer
     * animation
     * rendering
     * motion
     * sound
     * song
  new/del time      <-  new/delTimer (returns ID)
  inflate genome    <-  inflateGenome (including scene; should automate window creation too, that'd be sweet)
  roll dice         <-  rollDice

                        FIND
                        ====
  \Trigger::send     -> getUpdates( Bridge* )
                        ----------
                        Shunts to individual entities' metamethods:
                          \ * onInput (global, shunts to entity-specific onInput map)
                          * onTimeout
                          * onCollision

+=======+
|TRIGGER|
+=======+
  \INPUT
  ======
    windows back in working order
      \ Dialogue back in working order
        \ Dialogue's update() needs to be moved a render() function
      \Menu back in working order
        \inherit menu to text menu
      \Grid back in working order
        \z-layers (just use vectors. It'll be okay, nobody GAF. Only change it when you absolutely need to.)
          \each layer can have a BG and FG

  \TIMER
  ======
    \ timers flow through Trigger to callback

  \COLLISION
  ==========
    \any entity, doesn't have to be in top window
    \flow through Trigger to callback

  SCRIPTING
  =========
    implement all linkages*

  ORCHESTRA
  =========
    everything flows through Trigger
    TODO distinguish onInputWindow from onInput[Entity]
    
  LOADING
  =======
    * GENOMES: loading entities from genomes and passing their IDs to lua-side
    * INFLATE: reuse inflate() from jb
    * GRAPHS:  hooking scenes together through graph

  BUMP ALLOCATOR
  ==============
    * Is this even that helpful for this engine? I suspect not.

  UNFINISHED SUBSYSTEMS
  =====================
    * motion: It seems like motion can have another function after normal pos += vel to see if entities with destinations have reached ( or passed ) them yet with edits for gone-pasts.
    * animation
    * rendering (maybe this shouldn't be a subsystem so much as a start/stop/pause/unpause of Image visibility
    * motion
    * sound
    * song

------------------------


+====+
|MISC|
+====+
  - SYSTEMS:
    --------
    animation
    motion + collision system
    motion-only system (no rects needed maybe? idk...)
      this might afford different sorts of motion in one system. With acceleration
      will this be on a destination basis or raw kinematics?
    sound system ( you can already *play* sound, but you need something to manage 

  - FEATURES:
    ---------
    Support timer IDs pointing to which BG tile to animate next 
    tile-based animations w/ timers
    parallax (ditto)
    auto-looping bg
    camera following entity, should be easy to switch
    inherit menu to entity menu (later w/ grid)
    notify uncollisions as well
    lerping
    place mobile entities at the front of collision system
    Z-ordering-- use q-sorting, or some kind of built-in sorter
    bump allocator
    in-memory resources
    *my* map
    graph-based scene traversal; seems clearer now that each scene appears to be an entity in itself
    bar
    dice
    resource-loading

testing:
  remake Battle to test and refine engine
  go to Battle node on contact with enemy; return when done with enemy disappearing
  go from one room to the other

